name: CI

on:
  push:
    branches:
      - main
      - "feature/**"
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    name: Build & Unit Test
    runs-on: ubuntu-latest

    steps:
      - name: コードをチェックアウト
        uses: actions/checkout@v4

      - name: Go をセットアップ
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true

      - name: 依存関係を確認 (go mod tidy)
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: ビルド
        run: go build ./...

      - name: 静的解析 (go vet)
        run: go vet ./...

      - name: ユニットテスト実行
        # -tags=e2e を付けないことで E2E テストを除外する
        # -race は Linux では CGO 必須のため省略（セキュリティより CI 安定性を優先）
        run: go test -coverprofile=coverage.out ./internal/... ./pkg/...

      - name: カバレッジレポート
        run: go tool cover -func=coverage.out

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: コードをチェックアウト
        uses: actions/checkout@v4

      - name: Go をセットアップ
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true

      - name: golangci-lint を実行
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.10.1
          args: --timeout=5m
