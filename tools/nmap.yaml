name: nmap
binary: nmap
description: "ポートスキャン・サービス・OS検出"
tags: [recon, port-scan]
timeout: 600
args_template: "{flags} -p {ports} {target!}"
output:
  strategy: head_tail
  head_lines: 50
  tail_lines: 30
